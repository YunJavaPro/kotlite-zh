= 高级用法

== 工具结构

Kotlite 中有几个关键工具。执行方向在此图表中大致呈现。

[mermaid]
----
flowchart LR
词法分析器 <--> 解析器 --> 语义分析["语义分析器"] --> 解释器
语义分析 --> 代码生成["代码生成器（可选）"]
----

NOTE: 代码生成器生成带有可选调试信息的 Kotlin 代码。主要用于调试目的。

数据流如下：

[mermaid]
----
flowchart TD
Kotlite代码["Kotlite 代码"] --构造--> 词法分析器 <--递归解析--> 解析器
语义分析["语义分析器"]
执行环境["执行环境"]
解释器
语义分析 --> 代码生成["代码生成器"]
解析器 --生成--> Mermaid["Mermaid 图表代码"]
解析器 --生成--> AST节点["AST 节点"]
代码生成 --生成--> 调试代码["可调试 Kotlin 代码"]

AST节点 -- "由...预处理" --> 语义分析
AST节点 -- "经过预处理后，由...执行" --> 解释器

筛选["自定义筛选"] --构造--> 执行环境
自定义["自定义函数/属性/扩展"] --写入到--> 执行环境
语义分析 <-- 操作 --> 执行环境
执行环境 -- 由...读取 --> 解释器

解释器 -- 写入到 --> 符号表["符号表"]

style Kotlite代码 fill:#f62
style Mermaid fill:#aa2
style AST节点 fill:#aa2
style 筛选 fill:#f62
style 自定义 fill:#f62
style 调试代码 fill:#aa2
style 执行环境 fill:#aa2
style 符号表 fill:#aa2
----

所有工具类都标记为 `open`。开发人员可以自由扩展这些工具来创建新功能。

== AST 节点

解析器生成抽象语法树 (AST) 节点。解析器的函数与 https://kotlinlang.org/docs/reference/grammar.html[Kotlin 语法] 中定义的每个语法相对应。函数名称即为语法名称。

例如，调用 `Parser.script()` 根据 Kotlin 语法中的 `script` 定义解析代码，而 `Parser.type()` 根据 `type` 语法解析代码。

下面的 Kotlin 代码将字符串中编写的类型解析为语义分析器和其他工具可以使用的 AST 节点。

[source, kotlin]
----
val simpleTypeNode: ASTNode = Parser(Lexer("(anything)", "Double")).type()

val complexTypeNode: ASTNode = Parser(Lexer("(anything)", "Map<String, MutableList<Pair<Int, Double>>>")).type()

val genericTypeNode: ASTNode = Parser(Lexer("(anything)", "Map<K, V>")).type()

val functionTypeNode: ASTNode = Parser(Lexer("(anything)", "Int.(T) -> Unit")).type()
----

下面的 Kotlin 代码将字符串中编写的表达式解析为语义分析器和其他工具可以使用的 AST 节点。

[source, kotlin]
----
val expressionNode: ASTNode = Parser(Lexer("(anything)", "(1 + 2 * 3) / 4")).expression()
----

将其传递给语义分析器和解释器并手动执行。

[source, kotlin]
----
val executionEnvironment = ExecutionEnvironment()
SemanticAnalyzer(
    rootNode = expressionNode,
    executionEnvironment = executionEnvironment,
).analyze()
val result = Interpreter(
    rootNode = expressionNode,
    executionEnvironment = executionEnvironment,
).eval() as IntValue
val intResult: Int = result.value // 1
----
